#!/bin/bash

FILE='restart_bot'
php $HOME/bitmex-alerts-bot/index.php &
pid=`echo $!`
while true
do
    if [ $((`date +%s` - `stat -c %Z messages_ids.json`)) > 400 ]; then
        kill -9 $pid
        php $HOME/bitmex-alerts-bot/index.php &
        pid=`echo $!`
    fi
    sleep 1
done
