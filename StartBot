#!/bin/bash

while true
do
    pid=`ps -aux | grep "php index.php" | awk '{ print $2}'`
    pid=`echo $pid | awk '{ print $1}'`
    kill -9 $pid
    php $HOME/bitmex-alerts-bot/index.php &
    sleep 14400
done
